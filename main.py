from args import get_args
from crawler import Crawler

if __name__ == '__main__':
    args = get_args() 
    crawler = Crawler()
    #contents = crawler.crawl(args.start_date, args.end_date)
    # TODO: write content to file according to spec
   # with open(args.py) as args:

